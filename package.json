{
  "name": "form-validator",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc && dts-bundle --name form-validator --main dist/index.d.ts && find . -name '*.d.ts' ! -name 'form-validator.d.ts' -exec rm -f {} +",
    "test": "pnpm build && vitest"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "type": "module",
  "devDependencies": {
    "vitest": "^2.1.5"
  }
}
